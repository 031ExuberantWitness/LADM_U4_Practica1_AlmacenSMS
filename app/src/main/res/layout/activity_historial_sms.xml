<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="@color/white"
    android:theme="@style/Theme.LADM_U4_Practica1_AlmacenSMS"
    tools:context=".HistorialSMS">

    <LinearLayout android:id="@+id/llTitulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal">

        <ImageButton android:id="@+id/btnRegresar"
            android:layout_width="100dp"
            android:layout_height="match_parent"
            android:src="@drawable/regresar"
            android:background="@color/white"
            />

        <TextView android:id="@+id/lblTitulo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Historial de mensajes"
            android:textSize="30dp"
            android:textColor="#9E2730"
            android:fontFamily="@font/comforta"
            />

    </LinearLayout>

    <ListView android:id="@+id/listaMensajes"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/llTitulo"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="0dp"
        android:background="#9E2730"
        />

</RelativeLayout>